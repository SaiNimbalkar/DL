apiVersion: v1
kind: Service
metadata:
  name: flask-auto-service
spec:
  type: NodePort
  selector:
    app: flask-auto-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30008